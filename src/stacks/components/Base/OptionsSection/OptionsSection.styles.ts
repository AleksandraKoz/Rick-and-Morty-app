import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  optionRow: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 8,
  },
  iconMargins: {
    marginTop: 4,
    marginRight: 4
  },
  optionMargin: {
    marginBottom: 24
  }
});
